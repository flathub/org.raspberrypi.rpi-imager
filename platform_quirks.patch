diff --git a/src/linux/platformquirks_linux.cpp b/src/linux/platformquirks_linux.cpp
index 8d7ae976..4214af93 100644
--- a/src/linux/platformquirks_linux.cpp
+++ b/src/linux/platformquirks_linux.cpp
@@ -1465,8 +1465,8 @@ void clearAppImageEnvironment() {
     //
     // This is safe because forked children running external tools don't need
     // our bundled libraries.
-    unsetenv("LD_LIBRARY_PATH");
-    unsetenv("LD_PRELOAD");
+    //unsetenv("LD_LIBRARY_PATH");
+    //unsetenv("LD_PRELOAD");
 }
 
 qreal detectTextScaleFactor()
diff --git a/src/test/platformquirks_test.cpp b/src/test/platformquirks_test.cpp
index dc223800..267c7ea0 100644
--- a/src/test/platformquirks_test.cpp
+++ b/src/test/platformquirks_test.cpp
@@ -236,11 +236,11 @@ TEST_CASE("findCACertBundle returns valid path or nullptr", "[platformquirks][li
     }
 }
 
-TEST_CASE("clearAppImageEnvironment doesn't crash", "[platformquirks][linux]") {
-    // This is safe to call even outside an AppImage
-    // It just unsets LD_LIBRARY_PATH and LD_PRELOAD
-    REQUIRE_NOTHROW(PlatformQuirks::clearAppImageEnvironment());
-}
+//TEST_CASE("clearAppImageEnvironment doesn't crash", "[platformquirks][linux]") {
+//    // This is safe to call even outside an AppImage
+//    // It just unsets LD_LIBRARY_PATH and LD_PRELOAD
+//    REQUIRE_NOTHROW(PlatformQuirks::clearAppImageEnvironment());
+//}
 
 TEST_CASE("getBundlePath resolves executable outside AppImage", "[platformquirks][linux]") {
     // When not running from an AppImage, getBundlePath falls back to /proc/self/exe
